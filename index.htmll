<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Lily Gast — Portfolio</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,400;0,9..144,500;0,9..144,600;1,9..144,400&family=Work+Sans:wght@400;500;600&family=IBM+Plex+Mono:wght@400;500&display=swap" rel="stylesheet">
<style>
  :root {
    --bg: #F7F8FA;
    --bg-raised: #ECEFF5;
    --ink: #12151C;
    --muted: #6B7280;
    --line: #DFE3EA;
    --accent: #2F5CFF;
    --accent-soft: #7C9070;
    --tech: #2F5CFF;
    --card: #FFFFFF;
    --mono: 'IBM Plex Mono', monospace;
  }

  * { box-sizing: border-box; }
  html { scroll-behavior: smooth; }

  body {
    margin: 0;
    background: var(--bg);
    color: var(--ink);
    font-family: 'Work Sans', sans-serif;
    line-height: 1.65;
    -webkit-font-smoothing: antialiased;
    overflow-x: hidden;
  }

  h1, h2, h3 {
    font-family: 'Fraunces', serif;
    font-weight: 500;
    margin: 0;
    letter-spacing: -0.01em;
  }

  a { color: var(--ink); text-decoration: none; }
  .link-accent {
    color: var(--accent);
    border-bottom: 1px solid transparent;
    transition: border-color 0.2s ease;
  }
  .link-accent:hover { border-color: var(--accent); }

  .wrap { max-width: 760px; margin: 0 auto; padding: 0 24px; }

  /* ---------- NAV ---------- */
  nav {
    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: 50;
    padding: 18px 24px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    background: rgba(247,248,250,0);
    backdrop-filter: blur(0px);
    transition: background 0.3s ease, backdrop-filter 0.3s ease, border-color 0.3s ease;
    border-bottom: 1px solid transparent;
  }
  nav.scrolled {
    background: rgba(247,248,250,0.88);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid var(--line);
  }
  .nav-name {
    font-family: 'Fraunces', serif;
    font-size: 17px;
    font-style: italic;
  }
  .nav-links { display: flex; gap: 28px; font-size: 14px; color: var(--muted); }
  .nav-links a:hover { color: var(--ink); }
  @media (max-width: 640px) { .nav-links { display: none; } }

  /* ---------- HERO ---------- */
  .hero {
    min-height: 90vh;
    display: flex;
    align-items: center;
    position: relative;
    padding-top: 60px;
  }
  .hero .wrap { position: relative; z-index: 2; }

  .hero-kicker {
    font-size: 15px;
    color: var(--accent);
    margin-bottom: 20px;
    opacity: 0;
    animation: riseIn 0.7s cubic-bezier(.2,.7,.3,1) forwards;
    animation-delay: 0.1s;
  }

  .hero h1 {
    font-size: clamp(40px, 6.8vw, 66px);
    line-height: 1.12;
    max-width: 13ch;
    opacity: 0;
    transform: translateY(14px);
    animation: riseIn 0.7s cubic-bezier(.2,.7,.3,1) forwards;
    animation-delay: 0.25s;
  }
  .hero h1 .it { font-style: italic; color: var(--accent); }

  .hero p.tagline {
    margin-top: 24px;
    font-size: 18.5px;
    color: var(--muted);
    max-width: 48ch;
    opacity: 0;
    transform: translateY(14px);
    animation: riseIn 0.7s cubic-bezier(.2,.7,.3,1) forwards;
    animation-delay: 0.45s;
  }

  .hero-links {
    margin-top: 32px;
    display: flex;
    gap: 24px;
    font-size: 15px;
    opacity: 0;
    transform: translateY(14px);
    animation: riseIn 0.7s cubic-bezier(.2,.7,.3,1) forwards;
    animation-delay: 0.6s;
  }

  @keyframes riseIn { to { opacity: 1; transform: translateY(0); } }

  .scroll-cue {
    position: absolute;
    bottom: 36px; left: 24px;
    font-size: 13px;
    color: var(--muted);
    display: flex; align-items: center; gap: 10px;
    opacity: 0;
    animation: fadeIn 0.6s ease forwards;
    animation-delay: 1s;
  }
  @keyframes fadeIn { to { opacity: 1; } }
  .scroll-cue .line { width: 28px; height: 1px; background: var(--muted); }

  /* ---------- HALFTONE DECORATIVE GRAPHIC ---------- */
  .halftone {
    position: absolute;
    top: 8%;
    right: -4%;
    width: 380px;
    height: 380px;
    z-index: 0;
    pointer-events: none;
  }
  .halftone circle { fill: var(--accent); }
  @media (max-width: 900px) { .halftone { display: none; } }

  /* ---------- REVEAL ---------- */
  .reveal {
    opacity: 0;
    transform: translateY(28px);
    transition: opacity 0.8s cubic-bezier(.2,.7,.3,1), transform 0.8s cubic-bezier(.2,.7,.3,1);
  }
  .reveal.in { opacity: 1; transform: translateY(0); }

  /* ---------- SECTIONS ---------- */
  section { padding: 100px 0; position: relative; }
  section.raised { background: var(--bg-raised); }
  section.raised::before, section.raised::after {
    content: ""; position: absolute; left: 0; right: 0; height: 1px; background: var(--line);
  }
  section.raised::before { top: 0; }
  section.raised::after { bottom: 0; }

  .section-label {
    font-size: 13px; color: var(--accent);
    margin-bottom: 16px; font-style: italic;
    font-family: 'Fraunces', serif;
  }

  section h2 { font-size: clamp(28px, 4vw, 38px); margin-bottom: 26px; max-width: 17ch; }
  section p { font-size: 17px; max-width: 62ch; color: var(--ink); }
  .muted { color: var(--muted); }

  /* ---------- ASIDE — the personal touch ---------- */
  .aside {
    margin-top: 36px;
    padding: 24px 26px;
    background: var(--card);
    border-left: 3px solid var(--accent-soft);
    font-size: 15.5px;
    color: var(--muted);
    max-width: 56ch;
  }
  .aside strong { color: var(--ink); font-weight: 500; }

  /* ---------- SKILLS ---------- */
  .skills-flow { display: flex; flex-wrap: wrap; gap: 12px; margin-top: 30px; }
  .skill-pill {
    border: 1px solid var(--line);
    background: var(--card);
    padding: 9px 16px;
    font-family: var(--mono);
    font-size: 13px;
    border-radius: 100px;
    color: var(--muted);
    transition: border-color 0.2s ease, color 0.2s ease, transform 0.2s ease;
  }
  .skill-pill:hover { border-color: var(--tech); color: var(--ink); transform: translateY(-2px); }

  /* ---------- WAVEFORM DIVIDER ---------- */
  .waveform {
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 40px;
    margin: 0 0 8px;
  }
  .waveform span {
    display: block;
    width: 3px;
    background: var(--tech);
    border-radius: 2px;
    opacity: 0.55;
    transform-origin: center;
  }
  @keyframes waveDance {
    from { transform: scaleY(0.5); }
    to { transform: scaleY(1.25); }
  }

  /* ---------- CUSTOM PLAY BUTTON (drives hidden Spotify player) ---------- */
  .player-row {
    display: flex;
    align-items: center;
    gap: 14px;
    margin-top: 26px;
  }
  .play-btn {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    border: 1px solid var(--line);
    background: var(--card);
    color: var(--accent);
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    flex-shrink: 0;
    transition: border-color 0.2s ease, transform 0.15s ease, background 0.2s ease;
  }
  .play-btn:hover { border-color: var(--accent); transform: scale(1.06); }
  .play-btn.playing { background: var(--accent); color: #fff; border-color: var(--accent); }
  .spotify-caption {
    font-family: var(--mono);
    font-size: 12px;
    line-height: 1.5;
    color: var(--muted);
  }

  /* ---------- PROJECT ---------- */
  .project { margin-top: 40px; }
  .project-title { font-size: clamp(24px, 3.4vw, 30px); max-width: 19ch; margin-bottom: 18px; }
  .project-meta { display: flex; gap: 16px; font-family: var(--mono); font-size: 12.5px; color: var(--tech); margin-bottom: 28px; }

  .project blockquote {
    margin: 28px 0;
    padding: 0 0 0 22px;
    border-left: 2px solid var(--accent);
    color: var(--muted);
    font-style: italic;
    font-size: 17px;
    max-width: 56ch;
  }

  .signal-row {
    display: grid; grid-template-columns: 30px 1fr; gap: 18px;
    padding: 22px 0; border-top: 1px solid var(--line); align-items: baseline;
  }
  .signal-row:last-of-type { border-bottom: 1px solid var(--line); }
  .signal-num {
    font-family: 'Fraunces', serif; font-style: italic;
    color: var(--accent); font-size: 17px;
  }
  .signal-row h3 {
    font-family: 'Work Sans', sans-serif; font-weight: 600; font-size: 16px; margin-bottom: 4px;
  }
  .signal-row p { font-size: 15px; color: var(--muted); margin: 0; }

  .project-cta {
    margin-top: 32px;
    display: inline-flex; align-items: center; gap: 10px;
    font-size: 15.5px; color: var(--ink);
    padding-bottom: 3px; border-bottom: 1px solid var(--line);
    transition: border-color 0.2s ease, gap 0.2s ease;
  }
  .project-cta:hover { border-color: var(--accent); gap: 14px; }
  .project-cta .arrow { color: var(--accent); transition: transform 0.2s ease; }
  .project-cta:hover .arrow { transform: translateX(3px); }

  /* ---------- EXPERIENCE ---------- */
  .exp-item {
    display: grid; grid-template-columns: 22px 1fr; gap: 20px;
    padding: 26px 0; border-top: 1px solid var(--line);
  }
  .exp-item:last-child { border-bottom: 1px solid var(--line); }
  .exp-marker {
    font-family: 'Fraunces', serif; font-style: italic; color: var(--accent); font-size: 17px;
  }
  .exp-item .role { font-weight: 600; font-size: 16.5px; }
  .exp-item .org { color: var(--muted); font-size: 14.5px; margin: 2px 0 10px; }
  .exp-item p { font-size: 15.5px; margin: 0; }

  /* ---------- FOOTER ---------- */
  footer { padding: 100px 0 90px; }
  footer h2 { font-size: clamp(30px, 5vw, 44px); max-width: 15ch; margin-bottom: 24px; }
  footer .contact-links { margin-top: 26px; font-size: 16px; display: flex; gap: 28px; flex-wrap: wrap; }
  footer .contact-links a {
    border-bottom: 1px solid var(--line); padding-bottom: 3px; transition: border-color 0.2s ease;
  }
  footer .contact-links a:hover { border-color: var(--accent); }
  .foot-note { margin-top: 70px; font-size: 13px; color: var(--muted); font-style: italic; }
</style>
</head>
<body>

  <nav id="navbar">
    <div class="nav-name">Lily Gast</div>
    <div class="nav-links">
      <a href="#about">About</a>
      <a href="#project">Work</a>
      <a href="#experience">Experience</a>
      <a href="#contact">Contact</a>
    </div>
  </nav>

  <section class="hero">
    <div class="wrap">
      <div class="hero-kicker">Hi, I'm Lily —</div>
      <h1>Curious <span class="it">by default.</span></h1>
      <p class="tagline">Information Systems student at the University of Florida with a focus on technology, process improvement, and user-centered problem solving.</p>
      <div class="hero-links">
        <a class="link-accent" href="mailto:lilygracegast@gmail.com">Email</a>
        <a class="link-accent" href="https://www.linkedin.com/in/lily-gast" target="_blank">LinkedIn</a>
        <a class="link-accent" href="https://github.com/lilygracegast" target="_blank">GitHub</a>
      </div>
      <div class="player-row">
        <button class="play-btn" id="playBtn" aria-label="Play Sweet by The Nancys">
          <svg id="playIcon" width="14" height="14" viewBox="0 0 14 14"><path d="M2 1L12 7L2 13V1Z" fill="currentColor"/></svg>
          <svg id="pauseIcon" width="14" height="14" viewBox="0 0 14 14" style="display:none"><rect x="2" y="1" width="3.2" height="12" fill="currentColor"/><rect x="8.8" y="1" width="3.2" height="12" fill="currentColor"/></svg>
        </button>
        <div class="spotify-caption">"Sweet" — The Nancys, on repeat<br>Swamp Records, UF's student-run label</div>
      </div>
      <div id="spotify-hidden" style="position:absolute; width:1px; height:1px; overflow:hidden; opacity:0; pointer-events:none;"></div>
    </div>
    <div class="halftone" id="halftone"></div>
    <div class="scroll-cue"><span class="line"></span> Scroll</div>
  </section>

  <div class="wrap">
    <div class="waveform" id="waveform"></div>
  </div>

  <section id="about">
    <div class="wrap reveal">
      <div class="section-label">About</div>
      <h2>A bit about how I work</h2>
      <p>I'm drawn to problems that sit between people and data. Understanding what someone actually needs, then building something that delivers it. Start with research, ground decisions in real evidence, and follow through until it's actually solved, not just sketched out.</p>

      <div class="aside">
        <strong>Most weeks, I'm somewhere between a spreadsheet and a show.</strong> I work with Swamp Records, UF's student-run label, on booking, budgeting, and the unglamorous logistics behind a good night of live music. Outside of that, I'm usually rock climbing, on a trail, or shooting film photos. Different rooms, same instinct: notice what's actually going on, then do something about it.
      </div>
    </div>
  </section>

  <section class="raised" id="skills">
    <div class="wrap reveal">
      <div class="section-label">Skills</div>
      <h2>What I bring</h2>
      <p class="muted">Currently developing SQL, Power BI, and Python, with a UX and accessibility lens shaped by instructional design work.</p>
      <div class="skills-flow">
        <div class="skill-pill">Excel</div>
        <div class="skill-pill">SQL</div>
        <div class="skill-pill">Power BI</div>
        <div class="skill-pill">Python</div>
        <div class="skill-pill">HTML / CSS</div>
        <div class="skill-pill">Accessibility auditing</div>
        <div class="skill-pill">Stakeholder communication</div>
        <div class="skill-pill">Process improvement</div>
        <div class="skill-pill">Research-driven problem framing</div>
      </div>
    </div>
  </section>

  <section id="project">
    <div class="wrap reveal">
      <div class="section-label">Featured Project</div>
      <h2>Fixing a real UX gap</h2>

      <div class="project">
        <div class="project-title">"People You May Know" — Letterboxd Friend-Discovery Prototype</div>
        <div class="project-meta">
          <span>Independent project</span><span>·</span><span>Python</span><span>·</span><span>UX Research</span>
        </div>
        <p>Letterboxd (30M+ users) offers only two ways to find friends on the platform: syncing an X/Twitter or Facebook account, or searching an exact username. No contact sync, no QR code, no algorithmic suggestions, confirmed directly in the company's own FAQ.</p>
        <blockquote>"I find Letterboxd pretty boring because I can't find any of my friends on there. They only offer integration with X which I don't use."</blockquote>
        <p>I designed and built a Python prototype for a multi-signal friend-suggestion algorithm, combining three signals into one weighted match score:</p>

        <div class="signal-row">
          <div class="signal-num">01</div>
          <div><h3>Taste similarity</h3><p>Jaccard similarity on shared highly-rated films</p></div>
        </div>
        <div class="signal-row">
          <div class="signal-num">02</div>
          <div><h3>Location proximity</h3><p>Same-city matching</p></div>
        </div>
        <div class="signal-row">
          <div class="signal-num">03</div>
          <div><h3>Mutual connections</h3><p>Overlap in who users already follow</p></div>
        </div>

        <p class="muted" style="margin-top:24px;">This mirrors how real recommendation systems (LinkedIn, Instagram) blend multiple signals rather than relying on one. In testing, it surfaced strong matches that a taste-only version missed entirely.</p>

        <a class="project-cta" href="https://github.com/lilygracegast/letterboxd-friend-match" target="_blank">
          View the code and full write-up on GitHub <span class="arrow">→</span>
        </a>
      </div>
    </div>
  </section>

  <section class="raised" id="experience">
    <div class="wrap reveal">
      <div class="section-label">Experience</div>
      <h2>Where I've worked</h2>

      <div class="exp-item">
        <div class="exp-marker">01</div>
        <div>
          <div class="role">Data Intern</div>
          <div class="org">Health Information Project</div>
          <p>Analyzed 4,500+ survey responses in Excel to identify patterns and inform program recommendations.</p>
        </div>
      </div>

      <div class="exp-item">
        <div class="exp-marker">02</div>
        <div>
          <div class="role">Instructional Design Assistant</div>
          <div class="org">University of Florida IT</div>
          <p>Worked directly with stakeholders to build accessible, user-focused course content.</p>
        </div>
      </div>

      <div class="exp-item">
        <div class="exp-marker">03</div>
        <div>
          <div class="role">Business Team Member</div>
          <div class="org">Swamp Records</div>
          <p>Supported the business side of a student-run record label at UF.</p>
        </div>
      </div>
    </div>
  </section>

  <footer id="contact">
    <div class="wrap reveal">
      <div class="section-label">Get in touch</div>
      <h2>Let's talk.</h2>
      <p class="muted">Open to internship opportunities in business analytics, technology, and UX-adjacent roles for Summer 2027.</p>
      <div class="contact-links">
        <a href="mailto:lilygracegast@gmail.com">lilygracegast@gmail.com</a>
        <a href="https://www.linkedin.com/in/lily-gast" target="_blank">LinkedIn</a>
        <a href="https://github.com/lilygracegast" target="_blank">GitHub</a>
      </div>
    </div>
  </footer>

<script src="https://open.spotify.com/embed/iframe-api/v1" async></script>
<script>
  let spotifyController = null;
  window.onSpotifyIframeApiReady = (IFrameAPI) => {
    const element = document.getElementById('spotify-hidden');
    const options = {
      uri: 'spotify:track:4dUj221VHz0oN9PRnHxVo5',
      width: '1',
      height: '1'
    };
    IFrameAPI.createController(element, options, (controller) => {
      spotifyController = controller;
      controller.addListener('playback_update', e => {
        const playing = !e.data.isPaused && !e.data.isBuffering;
        updatePlayState(playing);
      });
    });
  };

  const playBtn = document.getElementById('playBtn');
  const playIcon = document.getElementById('playIcon');
  const pauseIcon = document.getElementById('pauseIcon');

  function updatePlayState(playing) {
    playBtn.classList.toggle('playing', playing);
    playIcon.style.display = playing ? 'none' : 'block';
    pauseIcon.style.display = playing ? 'block' : 'none';
    document.querySelectorAll('#waveform span').forEach((bar, i) => {
      bar.style.animation = playing
        ? `waveDance 0.8s ease-in-out ${(i % 6) * 0.08}s infinite alternate`
        : 'none';
    });
  }

  playBtn.addEventListener('click', () => {
    if (spotifyController) spotifyController.togglePlay();
  });
</script>

<script>
  // Generate halftone dot-pattern graphic — abstract organic blob, dot size
  // fades from center to edge (classic halftone technique)
  const halftoneContainer = document.getElementById('halftone');
  const hSize = 380;
  const svgNS = "http://www.w3.org/2000/svg";
  const svg = document.createElementNS(svgNS, "svg");
  svg.setAttribute("viewBox", `0 0 ${hSize} ${hSize}`);
  svg.setAttribute("width", "100%");
  svg.setAttribute("height", "100%");

  // Blob center + organic radius function (wavy circle via sin)
  const cx = hSize * 0.52, cy = hSize * 0.48;
  function blobRadius(angle) {
    return 150 + 34 * Math.sin(angle * 3.1) + 20 * Math.sin(angle * 5.3 + 1.2);
  }

  const grid = 13; // spacing between dot centers
  for (let x = 0; x <= hSize; x += grid) {
    for (let y = 0; y <= hSize; y += grid) {
      const dx = x - cx, dy = y - cy;
      const dist = Math.sqrt(dx * dx + dy * dy);
      const angle = Math.atan2(dy, dx);
      const boundary = blobRadius(angle);
      if (dist < boundary) {
        // Dot size fades from center (large) to edge (small)
        const t = dist / boundary; // 0 = center, 1 = edge
        const r = Math.max(0.6, 4.8 * (1 - t * t));
        const opacity = 0.85 - t * 0.5;
        const circle = document.createElementNS(svgNS, "circle");
        circle.setAttribute("cx", x);
        circle.setAttribute("cy", y);
        circle.setAttribute("r", r.toFixed(2));
        circle.setAttribute("opacity", opacity.toFixed(2));
        svg.appendChild(circle);
      }
    }
  }
  halftoneContainer.appendChild(svg);

  const waveform = document.getElementById('waveform');
  const barCount = 60;
  for (let i = 0; i < barCount; i++) {
    const bar = document.createElement('span');
    const h = 6 + Math.random() * 34;
    bar.style.height = h + 'px';
    waveform.appendChild(bar);
  }

  const nav = document.getElementById('navbar');
  window.addEventListener('scroll', () => {
    if (window.scrollY > 40) nav.classList.add('scrolled');
    else nav.classList.remove('scrolled');
  });

  const revealEls = document.querySelectorAll('.reveal');
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('in');
        observer.unobserve(entry.target);
      }
    });
  }, { threshold: 0.15 });
  revealEls.forEach(el => observer.observe(el));
</script>

</body>
</html>
